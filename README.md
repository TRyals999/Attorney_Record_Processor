# Attorney-Record-Processor
A command‑line C application that reads structured input, stores records in a dynamically allocated linked data structure, and supports flexible output and sorting options.
